<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.layout.mapper.LayoutShiftsMapper">
    
    <resultMap type="LayoutShifts" id="LayoutShiftsResult">
        <result property="id"    column="id"    />
        <result property="shiftName"    column="shift_name"    />
        <result property="starttime"    column="starttime"    />
        <result property="endtime"    column="endtime"    />
        <result property="shiftType"    column="shift_type"    />
        <result property="description"    column="description"    />
    </resultMap>

    <sql id="selectLayoutShiftsVo">
        select id, shift_name, starttime, endtime, shift_type, description from layout_shifts
    </sql>

    <select id="selectLayoutShiftsList" parameterType="LayoutShifts" resultMap="LayoutShiftsResult">
        <include refid="selectLayoutShiftsVo"/>
        <where>  
            <if test="shiftName != null  and shiftName != ''"> and shift_name like concat('%', #{shiftName}, '%')</if>
            <if test="starttime != null "> and starttime = #{starttime}</if>
            <if test="endtime != null "> and endtime = #{endtime}</if>
            <if test="shiftType != null  and shiftType != ''"> and shift_type = #{shiftType}</if>
            <if test="description != null  and description != ''"> and description = #{description}</if>
        </where>
    </select>
    
    <select id="selectLayoutShiftsById" parameterType="Long" resultMap="LayoutShiftsResult">
        <include refid="selectLayoutShiftsVo"/>
        where id = #{id}
    </select>

    <insert id="insertLayoutShifts" parameterType="LayoutShifts">
        insert into layout_shifts
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="shiftName != null and shiftName != ''">shift_name,</if>
            <if test="starttime != null">starttime,</if>
            <if test="endtime != null">endtime,</if>
            <if test="shiftType != null and shiftType != ''">shift_type,</if>
            <if test="description != null">description,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="shiftName != null and shiftName != ''">#{shiftName},</if>
            <if test="starttime != null">#{starttime},</if>
            <if test="endtime != null">#{endtime},</if>
            <if test="shiftType != null and shiftType != ''">#{shiftType},</if>
            <if test="description != null">#{description},</if>
         </trim>
    </insert>

    <update id="updateLayoutShifts" parameterType="LayoutShifts">
        update layout_shifts
        <trim prefix="SET" suffixOverrides=",">
            <if test="shiftName != null and shiftName != ''">shift_name = #{shiftName},</if>
            <if test="starttime != null">starttime = #{starttime},</if>
            <if test="endtime != null">endtime = #{endtime},</if>
            <if test="shiftType != null and shiftType != ''">shift_type = #{shiftType},</if>
            <if test="description != null">description = #{description},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteLayoutShiftsById" parameterType="Long">
        delete from layout_shifts where id = #{id}
    </delete>

    <delete id="deleteLayoutShiftsByIds" parameterType="String">
        delete from layout_shifts where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>